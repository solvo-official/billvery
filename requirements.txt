# Runtime dependencies of the Vercel Python Function (api/index.py).
# Kept in step with [project.dependencies] in pyproject.toml, which local development uses.
# Note: psycopg2 is deliberately absent -- this service talks to PostgreSQL over asyncpg.
fastapi>=0.115,<1.0
uvicorn>=0.32,<1.0
sqlalchemy[asyncio]>=2.0.36,<3.0
asyncpg>=0.31,<1.0
alembic>=1.14,<2.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
pyjwt>=2.10,<3.0
httpx>=0.28,<1.0
google-genai>=2.0,<3.0
python-multipart>=0.0.20
